const express = require("express");
const router = express.Router();
// const { pajakControllers } = require("../controllers");

// router.post("/cek", pajakControllers.cekPajak);

module.exports = router;
